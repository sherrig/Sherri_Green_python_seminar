from __future__ import division, print_function
from visual import *
charge = sphere()
charge.radius = 1
charge.pos = vector(1, 0, 0)
charge.color = color.green
charge2 = sphere(pos = vector(1, 0, 0), radius = 2,color = color.red, opacity = 0.5)

